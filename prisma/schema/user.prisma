model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  password  String // Storing password hash
  createdAt DateTime @default(now()) @map("created_at")

  @@map("users")
}
